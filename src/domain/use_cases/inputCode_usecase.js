import { MyResponse } from "../models/MyResponse"

export default class acceptMatchUseCase(code) {
    // 1. check user exist
    // 2. if true, check consulting end time
    // 3. if consulting doesn't finish, read user
}