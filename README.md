# LoveMatching

Next.js 기반 러브매칭 웹 앱입니다.

## 실행

```bash
npm install
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000) 을 엽니다.

## 빌드

```bash
npm run build
npm start
```

## 환경 변수

`.env.local` 파일에 Firebase API 키를 설정하세요.

```
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
```
